# Дизайн-ревью Knopik Tap

Полный кросс-дисциплинарный обзор по набору скилов
[`jakubkrehel/skills`](https://github.com/jakubkrehel/skills) с последующей
реализацией правок.

## Scope and Coverage

**Режим:** `full` (реализация запрошена, поэтому ревью не read-only).

**Область:** игровой экран (`app/page.tsx`), нижняя навигация, экран
«Настройки», общие поверхности листов. Область сужена до этих трёх поверхностей,
потому что запрос центрируется на них; кейс-анимации, мини-игры и рулетка
осмотрены только на предмет побочных эффектов от правок стекла и не
пересматривались целиком.

**Стек:** Next 16 (vinext / RSC), React 19, обычный CSS в трёх каскадных слоях
(`globals.css` → `interface-v2.css` → новый `interface-v3.css`). Tailwind
подключён в devDependencies, но интерфейс написан на чистом CSS — правки
сделаны в этой же идиоме, вторая система стилей не вводилась.

**Документы конвенций:** `README.md` описывает только сборку и хостинг;
`CONTRIBUTING.md`, `AGENTS.md`, `CLAUDE.md`, дизайн-система или Storybook
отсутствуют. Единственный писаный ориентир — комментарии в
`interface-v2.css`, где предыдущий пасс уже заявлял минимализм, но фактически
оставил «liquid glass» и декоративные слои.

| Домен | Что осмотрено | Результат |
| --- | --- | --- |
| Accessibility | `page.tsx` (диалоги, табы, свитчи, формы), `use-dialog-a11y.ts`, hit-области, `prefers-reduced-motion` | 4 находки |
| Layout | Настройки (шапка, группы, карточки), нижнее меню, спейсер, 320px и `max-height: 740px` | 4 находки |
| Writing | Все строки экрана настроек, лейблы табов, ошибки, пустые состояния | 3 находки |
| Typography | Заголовки, размеры подписей, табличные цифры, `text-wrap` | 2 находки |
| Colors | Пары «текст / стекло» на тёмной, светлой и красной сценах, семантика акцента | 2 находки |
| UI | Нижнее меню, концентричные радиусы, тени, анимации, иконки | 4 находки |

## Findings

Все находки исправлены в этой ветке.

| # | Severity | Domain | Location | Before | After | Why |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | HIGH | Accessibility | `app/page.tsx:2872` (`navInert`) | Нижнее меню не inert при `settingsOpen` — фокус и скринридер уходят на табы под полноэкранным оверлеем | `settingsOpen` добавлен в `navInert`, меню скрыто классом `is-hidden` | Модальный диалог обязан делать фон inert; иначе Tab уводит фокус за пределы диалога |
| 2 | HIGH | Colors | `interface-v2.css:2940` (`.bottom-bar button`), `interface-v3.css` | `color: rgb(90 110 127 / 80%)` на полупрозрачном стекле → 3.2:1 на тёмной сцене | `rgb(16 42 67 / 76%)` на стекле 84% → ≥4.9:1 на тёмной, светлой и красной сценах | Неактивные табы — единственный текст меню; на тёмной сцене пара не проходила 4.5:1 |
| 3 | HIGH | Layout | `interface-v2.css:708` + `globals.css:4227` | `.settings-sheet .sheet-heading { position: sticky; top: 0 }` — заголовок и крестик прибиты к верху экрана | `.settings-topbar` — обычный блок в потоке, `position: static` | Прямое требование: шапка должна ехать с контентом. Sticky-шапка на 4/5 экранной высоты съедала вертикаль на компактных экранах |
| 4 | MEDIUM | Layout | `app/page.tsx` (экран настроек целиком) | Плоский список из `<p class="settings-section-title">` + разнотипных панелей, вложенность не выражена | Шесть `<section>` с `<h3>`, карточками и шкалой отступов 8/24px | Заголовок-абзац не создаёт структуру; группировка воздухом (2× между группами) заменила визуальный шум |
| 5 | MEDIUM | UI | `interface-v2.css:2740–2860` (`.bottom-bar`) | 4-слойный градиент, `::before` с 8-секундной анимацией `liquid-glass-drift`, `::after`, 5 теней, rim-glow, `mix-blend-mode: screen` | Одна стеклянная капсула: 1 фон, 1 тень, 1 бордер; псевдоэлементы сняты | Постоянная фоновая анимация под пальцем — расход внимания и GPU без обратной связи; минимализм требовал снять декор |
| 6 | MEDIUM | UI | `interface-v2.css:2973` (`nav-icon-pop`) | Иконка активного таба проигрывает 420ms `scale`-анимацию + `drop-shadow` при каждом переключении | Анимация снята, состояние читается цветом и толщиной штриха | Motion restraint: переключение таба — высокочастотное действие, кастомная анимация повторяет цену внимания каждый раз |
| 7 | MEDIUM | UI | `globals.css:3617`, `interface-v2.css:1433` | Иконки табов собраны из `border`/`::before`/`::after`/`<i>` — толщина штриха 1.7/1.9/2px вразнобой | Три inline-SVG с `currentColor`, единый `stroke-width: 1.6` (1.9 в активном) | Один набор иконок, один вес штриха; состояние — через CSS, а не через отдельные ассеты |
| 8 | MEDIUM | Accessibility | `app/page.tsx` (свитчи звука и вибрации) | `aria-label="Звук"` дублирует и сокращает видимый текст «🔊 Звук» | `aria-labelledby` указывает на видимый лейбл «Звуковые эффекты» | Видимый текст обязан входить в доступное имя, иначе голосовое управление не находит контрол |
| 9 | MEDIUM | Accessibility | `app/page.tsx` (форма входа) | Ошибка входа выводилась `role="alert"`, но поля не связаны с ней | `aria-invalid` + `aria-describedby="cloud-login-error"` на обоих полях | Ошибка должна объявляться при попадании фокуса в сломанное поле, а не только один раз |
| 10 | MEDIUM | Writing | `app/page.tsx` (кнопки и статусы настроек) | «🔐 Войти в аккаунт», «🚪 Выйти», «📖 Повторить обучение», «🗑️ Сбросить», спиннер-лейбл «…» | Эмодзи убраны, лейблы глагольные, ожидание проговорено: «Входим…», «Проверяем…», «Сохраняем…» | Эмодзи в лейбле попадает в доступное имя и не переводится; «…» не сообщает, что происходит |
| 11 | MEDIUM | Writing | `app/page.tsx` (подтверждение сброса) | «Удалить весь прогресс? Это нельзя отменить!» + кнопки «Отмена» / «Сбросить» | «Весь прогресс будет удалён без возможности восстановления.» + «Сбросить прогресс» | Деструктивное подтверждение: спокойный тон без восклицания, кнопка повторяет последствие |
| 12 | MEDIUM | Writing | `app/page.tsx` (статус аккаунта, пустое состояние промокодов) | «📱 На этом устройстве», «✓ Сохранено», «Нет промокодов» | «Прогресс хранится на этом устройстве», «Прогресс сохранён в облаке», «Пока нет ни одного кода. Создайте первый выше.» | Статус, читаемый без иконки; пустое состояние объясняет место и даёт следующий шаг |
| 13 | LOW | Typography | `interface-v2.css:773` (`.settings-section-title`) | `font-size: 8px`, `font-weight: 820`, `letter-spacing: .08em`, `text-transform: uppercase` | `12px`, `600`, обычный регистр | 8px капсом — нижняя граница читаемости; заголовок группы не обязан кричать |
| 14 | LOW | Typography | `app/page.tsx` (статистика, промокоды, сложность) | Числа пропорциональными цифрами, «прыгают» при обновлении | `font-variant-numeric: tabular-nums` на всех числовых значениях | Меняющиеся числа не должны менять ширину |
| 15 | LOW | UI | `interface-v2.css:783–800` | Карточка `border-radius: 21px` с внутренними элементами того же радиуса | Концентричная шкала: карточка 20px = ряд 14px + паддинг 6px | Совпадающие радиусы вложенных поверхностей — самая заметная причина «неаккуратности» |
| 16 | LOW | Colors | `interface-v2.css:975` | `.primary-action` залита градиентом `#667eea → #764ba2` — фиолетовый вне палитры | `--ui-blue` как единственная заливка основного действия | Один акцент на систему; случайный градиент вводил второе значение цвета |
| 17 | LOW | Accessibility | `interface-v3.css` (свитчи) | Свитч 52×32 — ниже рекомендованных 44px для касания | Псевдоэлемент растягивает зону нажатия до 44px по высоте | Видимый элемент может остаться компактным, зона нажатия — нет |

## Considered but rejected

| Кандидат | Почему отклонён |
| --- | --- |
| Перевести палитру в OKLCH | Проект последовательно использует hex + `rgb()/%`; миграция цветовой системы не входит в запрос, а точечный OKLCH создал бы вторую нотацию |
| Убрать `position: fixed` у нижнего меню | Комментарии в `interface-v2.css:4445` документируют реальный баг iOS PWA; это защитное решение, а не небрежность |
| Разбить `page.tsx` (4.7k строк) на модули | Реальная проблема сопровождения, но это рефакторинг архитектуры, а не дизайн-ревью; риск регрессий в игровой логике несоразмерен |
| Заменить «Играть» на «Главная» | «Играть» точнее описывает назначение таба в игре и согласуется с остальной лексикой |
| Свести три таба к двум | Магазин, игра и кейсы — три равноправных раздела; объединение спрятало бы содержимое ради формальной простоты |
| Добавить тёмную тему | Сцена уже меняет светлоту по состоянию Кнопика; отдельная тема потребовала бы пересборки всех пар контраста и выходит за рамки запроса |

## Verification

| Проверка | Результат |
| --- | --- |
| `npx tsc --noEmit` | Проходит, ошибок нет |
| `npm run lint` | 0 ошибок, 30 предупреждений — все предсуществующие `@next/next/no-img-element` |
| `npm test` (сборка + 32 теста, включая SSR-рендер) | 32/32 проходят |
| `npm run build` | Успешно; `.settings-topbar`, `.settings-groups`, `.nav-icon`, `.settings-card`, `.settings-button` присутствуют в собранном CSS |
| Контраст пар «текст / стекло» | Посчитан программно (WCAG 2, sRGB) для тёмной `#0d2233`, светлой `#f2f5f8` и красной `#c63c32` сцен: лейблы табов ≥4.9:1, активный ≥8.4:1, вторичный текст настроек ≥5.9:1 |
| Dev-сервер | `GET /` → 200, `interface-v3.css` отдаётся, ошибок PostCSS нет |

**Не проверено:** визуальный прогон в реальном браузере и просмотр анимаций на
10% скорости — в песочнице не удалось установить Chromium (загрузка
`cdn.playwright.dev` и `storage.googleapis.com` заблокирована сетью). Остаются
непроверенными: фактический рендер `backdrop-filter` поверх игровой сцены,
поведение на iOS PWA с home-indicator и раскладка при 200% зуме. Живой превью
запущен — эти три пункта стоит просмотреть глазами.

## Verdict

`Needs changes` → после реализации всех 17 находок актуальных находок не
осталось; статус ограничен пометкой **Not verified** по визуальному прогону
выше.

---

# Ревью №2 — после перестройки плашки балансов

Второй проход по тому же набору скилов
([`jakubkrehel/skills`](https://github.com/jakubkrehel/skills)), запрошенный
после реализации раунда 2. В отличие от первого прохода этот выполнен **в живом
браузере** — ограничение «Not verified» выше снято.

## Scope and Coverage

**Режим:** `full` (реализация запрошена, ревью не read-only).

**Область:** изменения раунда 2 — плашка балансов (`app/page.tsx:3580-3660`),
её стили (`app/interface-v3.css`, раздел «6. Плашка балансов»), зазор до
нижней навигации, переименование товара в магазине, нажимаемость всех кнопок.
Экраны настроек и кейсов осмотрены на предмет регрессий от правок стекла.

**Стек:** тот же — Next 16 (vinext / RSC), React 19, чистый CSS в трёх слоях.

**Документы конвенций:** без изменений — конвенций в репозитории нет,
единственный ориентир — комментарии в CSS.

**Граница:** правила игры, экономика и балансировка не трогались. Устройство
свайпа менялось только в части доступности; жест сохранён.

| Домен | Что осмотрено | Результат |
| --- | --- | --- |
| Accessibility | Роль и имя центрального элемента плашки, клавиатурный путь до перевода в сейф, hit-области 46 кнопок на пяти экранах, `prefers-reduced-motion` для блика | 2 находки |
| Layout | Зазор плашка → навигация на 320/375/390/414, перекрытия, порядок монет, отсутствие клиппинга списка магазина | 1 находка |
| Writing | Подписи «Актив», «В сейфе», «В сейф», `aria-label` кнопки, название «Тюбетейка» | 1 находка |
| Typography | Размеры и вес подписей и чисел плашки, табличные цифры | 0 находок |
| Colors | Попиксельный замер контраста подписей и чисел на тёмной и светлой сценах | 2 находки |
| UI | Концентричность радиусов, длительность и характер блика, press-состояние, статичный дубль подсказки направления | 1 находка |

## Findings

| # | Severity | Domain | Location | Before | After | Why |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | HIGH | Accessibility | `app/page.tsx:3611` | Центр плашки — `<div role="group" tabindex="0">` с ручным `onKeyDown`; скринридер объявлял «группа», не «кнопка», и не сообщал о действии | Настоящий `<button type="button">` с динамическим `aria-label` («Перевести N монет в сейф» / «Перевод в сейф недоступен»), `disabled={!canSave}`, `onClick` → `saveAllToVault()` | `better-accessibility`: нативный элемент вперёд ARIA; управляющий элемент обязан иметь роль и имя. Триггер «control with no accessible name» — эскалация до HIGH на месте |
| 2 | HIGH | Colors | `app/interface-v2.css:4074` → фикс `app/interface-v3.css:1072` | `.swipe-plate.is-locked { opacity: .72 }` гасил **весь** узел вместе с текстом: подписи «Актив» / «В сейфе» / «В сейф» давали **2.85:1** на светлой сцене | Прозрачность снята с узла (`opacity: 1`), приглушение перенесено на фон и монеты; замер стал **6.07:1** | `better-colors`: тело текста ≥4.5:1 (WCAG AA). Триггер «body text failing contrast» — HIGH. Один корень (родительская `opacity`), одна строка |
| 3 | MEDIUM | Colors | `app/interface-v3.css:1083-1093` | Подписи плашки наследовали `--adaptive-muted` 66% — 4.36:1 на светлой сцене даже без блокировки, впритык к порогу | Подняты до 78% чернил на светлых сценах и 84% белого на тёмных: **6.07:1** и **5.88:1** | `better-colors`: контраст правится светлотой при неизменных C и H; запас важнее точного попадания в порог |
| 4 | MEDIUM | Layout | `app/interface-v3.css:820-836` | Плашка вплотную подходила к плавающей навигации | Зазор **49-52px** на всех проверенных ширинах, `overlaps: false` | `better-layout`: межгрупповой зазор ≥2× внутригруппового; плавающие контролы не наезжают на контент |
| 5 | MEDIUM | Accessibility | `app/interface-v3.css:932-948` | Центральная зона была меньше рекомендованной тач-цели | Кнопка **68×48px** — выше пола WCAG 2.5.8 (24×24) и близко к рекомендации 44×44 по обеим осям | `better-accessibility` / `hit-areas`: рекомендованная тач-цель 44×44, расширять через сам контрол, не через `<input>` |
| 6 | MEDIUM | UI | `app/interface-v3.css:1001-1035` | Направление свайпа несла только анимация | Блик 3.6s ease-in-out + **статичный дубль**: стрелка и подпись «В сейф» остаются под `prefers-reduced-motion: reduce`, где блик выключается | `better-ui`: сдержанность анимации на частом взаимодействии + статичная подсказка; `better-accessibility`: движение обёрнуто в медиазапрос |
| 7 | LOW | Writing | `app/page.tsx:4076`, `app/case-engine.ts` | «Тюбетейка Хасбика» — отсылка к персонажу, не к предмету | «Тюбетейка» | `better-writing`: один словарь на поток, название описывает предмет |

## Considered but rejected

| Что | Почему отклонено |
| --- | --- |
| Оставить ручной `onKeyDown` на новой кнопке | `<button>` уже обрабатывает Enter и Space нативно; лишний обработчик дублирует поведение и не проходит типизацию (`KeyboardEventHandler<HTMLDivElement>`, TS2322) |
| Убрать свайп в пользу одной кнопки | Жест — часть игровой механики; доступность закрыта параллельным клавиатурным путём, а не заменой жеста |
| Поднять контраст через насыщенность монет | `better-colors` требует править светлоту, сохраняя C и H; монеты — смысловой цвет (золото = актив, серебро = сейф), их сдвиг ломает «один цвет — один смысл» |
| Заменить `opacity` на `filter: grayscale()` для заблокированной плашки | Тот же дефект: наследуется текстом. Приглушение адресно перенесено на фон и монеты |
| Анимировать саму стрелку вместо блика | `better-ui`: дрожащая иконка на высокочастотном взаимодействии — шум; направление лучше несёт проходящий блик |

## Verification

Прогон в реальном Chromium 149 (`@sparticuz/chromium`, headless shell) против
живого дев-сервера `http://localhost:3000/`.

| Проверка | Команда / действие | Результат |
| --- | --- | --- |
| Геометрия и перекрытия | `node final.mjs` на 320×640, 375×667, 390×844, 414×896 | Зазор плашка → навигация 49-52px, `overlaps: false` на всех |
| Нажимаемость всех кнопок | Обход home / settings / shop / clothes / cases, проверка `elementFromPoint` в центре каждой кнопки и её размеров | **46 кнопок: 0 перекрытых, 0 меньше 24px** |
| Клавиатурный путь | Фокус на центральную кнопку → `Enter` | Сейф `0 → 5` — перевод работает без мыши |
| Имя и роль контрола | Чтение `tag` / `aria-label` / `disabled` из DOM | `BUTTON`, «Перевести 5 монет в сейф» (активна) и «Перевод в сейф недоступен» (заблокирована), `disabled` переключается |
| Контраст | Скриншот 2×, декодирование PNG, выбор доминирующего фона и крайнего по светлоте текста в рамке каждого элемента, WCAG 2 sRGB | Светлая сцена: подписи **6.07:1**, числа **10.7:1**; тёмная: подписи **5.88:1**, числа **7.79:1**; табы 5.9-11.8:1 — **все проходят** |
| Блик реально анимируется | Замер `background-position-x` 14 раз за 3.6s | 8 различных значений — анимация идёт, не застывший градиент |
| Reduced motion | Эмуляция `prefers-reduced-motion: reduce` | `animationName: none`, стрелка и подпись «В сейф» на месте — смысл не потерян |
| Концентричность радиусов | Замер вычисленных стилей | Навигация 26 = элемент 20 + padding 6 — правило `outer = inner + padding` выполнено; плашка 22 / padding 8 |
| Переименование | Поиск «Хасбик» в DOM после перехода на вкладку «ОДЕЖДА» | `hasbik: false`, карточка «Тюбетейка» видна и не обрезана |
| Ошибки консоли | Сбор `console.error` и `pageerror` за весь прогон | `errors: []` |
| Типы, линт, тесты | `npx tsc --noEmit`, `npm run lint`, `npm test` | Чисто; 0 ошибок (29 предупреждений — предсуществующие `no-img-element`); **32/32 теста проходят** |

## Verdict

`Approve`. Обе находки уровня HIGH закрыты и перепроверены попиксельно в живом
браузере; открытых находок не осталось. Ограничение «Not verified» из первого
ревью снято: рендер, анимации, контраст и клавиатурный путь проверены
фактическим прогоном, а не по исходникам.
